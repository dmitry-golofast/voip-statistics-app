## App for getting call statistics from Asterisk, weather forecast and general information in telegram bot

### Setup
```
npm init -y
npm install axios node-telegram-bot-api pg pg-hstore sequelize
```
